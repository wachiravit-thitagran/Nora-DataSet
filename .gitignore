__pycache__/
*.pyc
.venv/
venv/
dist/
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
# Every variant, not just bare .env: the Jenkins deploy writes .env.production
# into the workspace and a plain ".env" line would not have matched it.
.env
.env.*

# local run artefacts
.local/

# macOS
.DS_Store
._*

# Staging area for files the Cowork bridge could not unlink. Local rubbish,
# never part of the project.
_to_delete/
